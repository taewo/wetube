extends layouts/main
include mixins/videoBlock

block content
    .videos
        each item in videos
            console.log(33, item)
            +videoBlock({
                id: item.id,
                videoFile:item.fileUrl,
                title: item.title,
                views: item.views
            })